<h1>Performance Import</h1>
<p>A CSV or Excel file can be used to import records. The first row should be the column name. The following column are allowed.</p>

<ul>
  <% Performance.columns.each do |column| %>
    <% if column.name.in? ["id", "date", "time", "room_id","show_id"] %>
      <li>
        <strong><%= column.name %></strong> -
        <%= column.type.to_s.titleize %> type
        <% if column.name == "date"%>
        (must be in format yyyy-mm-dd ie 2018-12-25)
        <% end %>
        <% if column.name == "time"%>
        (24hr format)
        <% end %>
      </li>
    <% end %>
  <% end %>
</ul>

<p>If an <strong>id</strong> is supplied it will update the matching record instead of creating a new one.</p>

<%= form_for @performance_import do |f| %>
  <% if @performance_import.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@performance_import.errors.count, "error")%> prohibited this import from completing:</h2>
      <ul>
      <% @performance_import.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.file_field :file %>
  </div>
  <div class="buttons"><%= f.submit "Import" %></div>
<% end %>

