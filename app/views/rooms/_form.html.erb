<%= form_for([@venue, @room]) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@room.errors.count, "error") %> prohibited this article from being saved:
      </h2>
      <ul>
        <% @room.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <p>
    <%= f.label :roomName %>
    <%= f.text_field :roomName %>
  </p>


  <p>
    <%= f.submit %>
  </p>
<% end %>
