<p>
  <h2><%= room.roomName %></h2>
  <%= link_to 'New Show', new_show_path %>
  <%= link_to 'Edit Room', edit_room_path(room)%>
  <%= link_to 'Delete Room', [room],
  method: :delete,
  data: {confirm: "Are you sure you want to delete this room and all it's shows and performances?"}%>


</p>
<table>
  <tr>
    <th>Artist</th>
    <th>Title</th>
  </tr>
  <%= render @shows %>

</table>
